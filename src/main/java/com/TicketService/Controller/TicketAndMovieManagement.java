package com.TicketService.Controller;

import org.springframework.stereotype.Controller;

@Controller
//@RequestMapping("/movie")
public class TicketAndMovieManagement {
	/*
	 * @Autowired private TicketFinder ticketFinder;
	 * 
	 * @RequestMapping(value = "/movie/findMovie**", method = RequestMethod.GET)
	 * public ModelAndView findMovie(@PathVariable("movieName") String
	 * movieName) { System.out.println("Yippe ka Yay Mother  Fucker");
	 * ModelAndView mav = new ModelAndView(); List<Movie> movies =
	 * ticketFinder.findByName(movieName); mav.setViewName("MovieList");
	 * mav.addObject("movieList", movies); return mav;
	 * 
	 * }
	 */
	
	
}
